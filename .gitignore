# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Node
node_modules/
# Copied out of @superdoc/docx-engine at predev, not authored here.
apps/web/public/superdoc/
.next/
out/
*.tsbuildinfo
coverage/

# Env & secrets
.env
.env.local
*.pem
*.key

# Data & artifacts
.scannerwork/
data/
uploads/
*.log

# Kept out of the repository by choice, not by accident. These are the build's
# own working material rather than the product: the agent instructions, the
# internal documentation, the operator scripts, the encrypted backups the
# backup script writes, the design reference the interface was built from,
# the scanner configuration and the source specification.
CLAUDE.md
docs/
# Anchored. A bare `scripts/` matches a directory of that name at any depth, so
# it also swallowed apps/web/scripts, which holds the SuperDoc worker sync that
# `prebuild` runs. The build then failed on a clone with a missing module and
# nothing to say where it went.
backups/
_design_ref/
sonar-project.properties
PRD - AI for Legal Workflows.pdf
templates/
akin/
# Source document, kept locally, never committed.
EqualyzAI VoiceAgents - DP Assessment DSN_DPIA.docx

# Development logs from scripts/dev.sh.
.dev/
PRD - AI for Legal Workflows.docx